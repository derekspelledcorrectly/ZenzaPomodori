name: check

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    name: just check
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build and quality-gate tools
        run: brew install xcodegen just swiftlint

      - name: Run quality gates
        run: just check
